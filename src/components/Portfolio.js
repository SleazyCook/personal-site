import React from 'react';
import {Link} from 'react-router-dom';

const Portfolio = () => {
  return (
    <div>
      <nav>
        {/* navbar to other portfolio links */}
        {/* personal, school, work, etc */}
        {/* what does portfolio landing look like? */}
        {/* do I need to set up a toggle? */}
      </nav>
    </div>
  )
}

export default Portfolio;